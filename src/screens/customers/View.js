import { SafeAreaView, ScrollView } from "react-native";
import CustomerView from "../../features/customers/View";
const ViewScreen = () => {
    return (
            <CustomerView/>
    );
};


export default ViewScreen;
